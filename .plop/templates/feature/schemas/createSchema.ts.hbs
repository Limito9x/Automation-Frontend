import { z } from "zod";

export const create{{pascalCase name}}Schema = z.object({
    name: z.string().min(1),
});

export type Create{{pascalCase name}}Input = z.input<typeof create{{pascalCase name}}Schema>;
export type Create{{pascalCase name}}Output = z.output<typeof create{{pascalCase name}}Schema>;
