import { Form, FormGrid, zodResolver, useForm } from "@/components/form";
import { FormInput } from "@/components/form-controls";
import { create{{pascalCase name}}Schema, type Create{{pascalCase name}}Input, type Create{{pascalCase name}}Output } from "../schemas/create{{pascalCase name}}Schema";
import { useTranslation } from "react-i18next";

interface {{pascalCase name}}FormProps {
    onSubmit: (data: Create{{pascalCase name}}Output) => void;
}

export function Create{{pascalCase name}}Form({ onSubmit }: {{pascalCase name}}FormProps) {
    const { t } = useTranslation("{{camelCase name}}s");
    const form = useForm<Create{{pascalCase name}}Input, any, Create{{pascalCase name}}Output>({
        resolver: zodResolver(create{{pascalCase name}}Schema),
        defaultValues: {
            name: "",
        }
    });

    return (
        <Form form={form} formId={"create-{{kebabCase name}}-form"} onSubmit={onSubmit}>
            <FormGrid cols={1}>
                <FormInput
                    control={form.control}
                    label={t("fields.name", { defaultValue: "Name" })}
                    name="name"
                    type="text"
                    placeholder="Enter name"
                />
            </FormGrid>
        </Form>
    );
}
